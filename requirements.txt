# Mirror of pyproject.toml for deploy targets that expect requirements.txt
# (Replit/Render). Local dev uses uv: `uv sync`, then `uv run ...`.
httpx>=0.27
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
anthropic>=0.40
python-dotenv>=1.0
# optional partner integration:
openai>=1.30
